---
- name: Verify local OpenShift cluster domains and agent ISO attachment
  hosts: hypervisors
  become: true
  gather_facts: false
  vars_files:
    - ../../vars/cluster/openshift_install_cluster.yml
    - ../../vars/guests/openshift_cluster_vm.yml
    - ../../vars/cluster/openshift_agent_media.yml
  roles:
    - openshift_cluster_verify

